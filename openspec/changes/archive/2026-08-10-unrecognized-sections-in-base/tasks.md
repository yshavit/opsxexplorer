## 1. Parser and model

- [x] 1.1 In `src/specs/model.rs`, add an `UnrecognizedSection { title: String, body: String }` struct; change `Delta.unrecognized_sections` from `Vec<String>` to `Vec<UnrecognizedSection>`; add the same-shaped `unrecognized_sections: Vec<UnrecognizedSection>` field to `Spec`
- [x] 1.2 In `parse_delta` (`src/specs/parse.rs`), change the fallback arm from pushing `other.to_string()` to pushing `UnrecognizedSection { title: other.to_string(), body: render_body(&doc.ctx, &section.body) }`
- [x] 1.3 In `parse_spec` (`src/specs/parse.rs`), change the fallback arm from `other => return Err(unrecognised_section_error(path, other))` to pushing the same `UnrecognizedSection { title, body }` shape into a local `Vec` and continuing the loop; populate `Spec.unrecognized_sections` from it
- [x] 1.4 Remove `unrecognised_section_error` and the now-unreachable `StructureErrorKind::UnrecognisedOperationSection` arm/variant from `src/specs/error.rs` if nothing else constructs it; otherwise leave the variant and just stop `parse_spec` from producing it
- [x] 1.5 Update `src/specs/parse.rs` tests: `unrecognised_operation_section_is_reported_for_spec_of_record` no longer holds (it currently asserts `parse_spec` returns `Err`) — replace with a test asserting `parse_spec` collects the section's title and body and keeps parsing; update the existing delta tests (`unrecognised_delta_section_among_well_formed_ones_is_collected`, `several_unrecognised_delta_sections_preserve_document_order`) to assert on `UnrecognizedSection` values (title and body) instead of bare strings; add a test asserting a `parse_delta`/`parse_spec` unrecognised section's body matches `render_body`'s output for that section

## 2. Diff

- [x] 2.1 In `src/diff/model.rs`, replace `CapabilityDiff.unrecognized_sections: Vec<String>` with two fields: `delta_unrecognized_sections: Vec<UnrecognizedSection>` and `base_unrecognized_sections: Vec<UnrecognizedSection>` (reusing `spec_model`'s `UnrecognizedSection` type, or a `diff`-local re-export of it)
- [x] 2.2 In `diff()` (`src/diff/mod.rs`), populate `delta_unrecognized_sections` from `pair.delta.unrecognized_sections` verbatim, and `base_unrecognized_sections` from `pair.base.as_ref().map(|b| b.unrecognized_sections.clone()).unwrap_or_default()`
- [x] 2.3 Update existing `diff/mod.rs` tests that reference `unrecognized_sections` to use the two new fields; add a test for a capability with no spec of record at all asserting `base_unrecognized_sections` is empty, and a test for a capability with unrecognised sections on both sides asserting both fields are populated independently

## 3. Rendering

- [x] 3.1 In `src/tui/diff_row.rs`, add an `Origin` enum (`Delta`, `Base`) and a `RowKey::UnrecognizedSection { capability: String, origin: Origin, index: usize }` variant, replacing `RowKey::UnrecognizedSections`
- [x] 3.2 Replace the `DiffRow::UnrecognizedSectionsHeading`/`UnrecognizedSections { titles, key }` variants with: two heading variants (or one parameterized by `Origin`) and one `UnrecognizedSection { title: &str, body: &str, expanded: bool, key: RowKey }` row variant per section, shaped like `DiffRow::Requirement` (collapsible, selectable, carries `expanded`/`key`)
- [x] 3.3 In `flatten()` (`src/tui/diff_row.rs`), after the `for req in &diff.requirements` loop: if `!diff.delta_unrecognized_sections.is_empty()`, push the delta-sourced heading followed by one row per section (index-keyed, `expanded` from the shared expanded-set); then, if `!diff.base_unrecognized_sections.is_empty()`, push the base-sourced heading and its rows the same way. Each row expanded reveals its body as a `DiffRow::Body`-shaped row (or equivalent), unstyled, with no further fits-or-collapses check
- [x] 3.4 In `src/tui/mod.rs`, replace `unrecognized_sections_box`/`unrecognized_sections_lines` with: a heading renderer taking a style and an optional italic subtitle line (delta-sourced: `unrecognised_style()`, no subtitle; base-sourced: `Style::default()`, subtitle "in the spec of record, not this change" or similar, styled unstyled+italic), both reading "Other sections" and routed through the shared `heading_box`; and a row renderer for one unrecognised section showing its title collapsed, or its title plus full unstyled body when expanded. Remove the `UNRECOGNIZED_SECTIONS_PROMPT` constant and its rendering entirely
- [x] 3.5 Wire the new row/heading variants into `build_diff_lines` (`src/tui/mod.rs`) the same way `GroupHeading`/`Requirement` are: headings skip the cursor, section rows are selectable and toggle their own `expanded` state on `Enter`/`Space`/`l`/`h` like a `Requirement` row does (not a no-op)
- [x] 3.6 Update `src/tui/diff_row.rs` and `src/tui/mod.rs` tests: replace the old flat-bullet-list tests with tests covering — a delta-sourced-only capability renders just the purple group; a base-sourced-only capability renders just the unstyled group with its subtitle; both present render delta's group first then base's; neither present renders nothing; each section row starts collapsed and expanding one reveals its full body with no truncation regardless of length; toggling a row is no longer a no-op; cursor movement skips both headings but stops on every section row, including via `Ctrl+d`/`Ctrl+u` scrolling; narrow-pane degrade applies to both headings; no row anywhere renders the old enhancement-request prompt text

## 4. Verification

- [x] 4.1 `cargo test`
- [x] 4.2 `cargo clippy`
- [x] 4.3 Manually exercise the pane (see project's `run` skill) against a change whose base spec.md (`openspec/specs/<capability>/spec.md`) has an extra unrecognised `##` section, confirming the capability's tab renders normally instead of a full-pane error, with a plain "Other sections" group at the bottom showing that section collapsed by default and expandable to its full body
- [x] 4.4 Manually exercise a change whose own delta spec.md has an unrecognised section too, confirming its purple "Other sections" group renders above the base-sourced one, with no enhancement-request prompt anywhere
