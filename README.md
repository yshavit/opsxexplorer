# opsxexplorer

## Development

This repo includes a sandboxed [dev container](.devcontainer/README.md) for
running Claude Code — repo-only disk access, default-deny network firewall.
See `.devcontainer/README.md` for setup (`just claude` to start a session).