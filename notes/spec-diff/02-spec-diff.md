# Brief: `spec-diff` capability — compare a delta requirement against its base

> Chain position: **2 of 3**. Read → compare → render.
> 1. `spec-model` (`01-spec-model.md`) — parse a spec.md; load both sides.
> 2. **`spec-diff`** (this file) — compute the requirement-level delta.
> 3. `tui-specdiff` (`03-tui-specdiff.md`) — render it in the right pane.

This brief has been implemented and archived. The source of truth is now
`openspec/changes/archive/2026-08-08-spec-diff/` (proposal.md, specs/,
design.md, tasks.md) and the resulting main spec at
`openspec/specs/spec-diff/spec.md`.

`notes/spec-diff/` is not deleted yet — that happens only once all three
changes in the chain (`spec-model`, `spec-diff`, `tui-specdiff`) have landed.
